## API3 DAO Web Client

- Setup:

      cp .env.example .env
      vim .env  # fill in the blanks
      yarn install

- Run dev server: `yarn start`

- Lint: `yarn lint`

- Make a production build: `yarn build`
